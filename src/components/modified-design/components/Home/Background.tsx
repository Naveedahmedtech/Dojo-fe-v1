import React from 'react';

const Background: React.FC<{ backgroundImage?: string }> = ({  }) => {
    return (
        <div  />
        // <div className="absolute top-0 left-0 w-full h-full" style={{ backgroundImage: `url(${backgroundImage})`, backgroundPosition: 'center', opacity: 0.3 }} />
    );
};

export default Background;
